const chai = require('chai');

const expect = chai.expect;

describe('A vue countdown component', () => {
  beforeEach(() => {});
  afterEach(() => {});
  it('should pass', () => expect(true).to.be.true);
});
