import chai from 'chai';
import Vue from 'vue';
import Clock from '../src/Clock.vue';

const expect = chai.expect;

describe('A vue clock component', () => {
  beforeEach(() => {});
  afterEach(() => {});
  it('should pass', () => expect(true).to.be.true);
});
